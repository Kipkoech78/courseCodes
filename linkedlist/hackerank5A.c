#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<stdbool.h>
linkecreated( int n);
struct node 
    {
        int data;
        struct node *next;
    }node;
int main()
{
    int n;
    printf("enter number of elementds you want to feed into linked list");
    scanf("%d", n);
    linkecreated(n);
}
linkecreated(int n)
{
    int i=0;
    int *head = NULL;
    int 


}